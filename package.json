{
  "name": "@micro-js/map-obj",
  "description": "Map for objects.",
  "repository": "git://github.com/micro-js/map-obj.git",
  "version": "1.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@micro-js/foreach": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2",
    "standard": "^5.4.1"
  }
}
